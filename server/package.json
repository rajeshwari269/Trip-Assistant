{
  "name": "trip-assistant-server",
  "version": "1.0.0",
  "description": "Backend server for Trip Assistant application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup-env": "node setup-env.js",
    "security-audit": "node security-audit.js",
    "validate-env": "node -e \"console.log(require('./utils/envValidator').validateEnvironment())\"",
    "prestart": "node -e \"require('./utils/envValidator').validateEnvironment()\""
  },
  "dependencies": {
    "axios": "^1.11.0",
    "bcryptjs": "^2.4.3",
    "chalk": "^4.1.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.8.7",
    "multer": "^1.4.5-lts.1",
    "pexels": "^1.4.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
